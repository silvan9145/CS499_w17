{
  "name": "buslivemap",
  "version": "1.0.0",
  "description": "Web-service for mapping the Bronco Shuttle",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.12.0",
    "request": "^2.79.0"
  }
}
